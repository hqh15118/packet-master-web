myconfig:
  #global_address : 192.168.1.106
  #global_address : 192.168.0.114
  global_address : 10.180.81.207
  #global_address : 192.168.0.121
  admin_server_port : 8989
  packet_master_web_port : 8080
  kafka-server-url : 10.15.191.100:9092
  openAOPLog : true
  sendAopLog : false

server:
  port: ${myconfig.packet_master_web_port}
  address: ${myconfig.global_address}

spring:
  application:
    name: packet-master-web
  datasource:
    dynamic:
      # 是否开启 SQL日志输出，生产环境建议关闭，有性能损耗
      p6spy: false
      hikari:
        connection-timeout: 30000
        max-lifetime: 1800000
        max-pool-size: 15
        min-idle: 5
        connection-test-query: select 1
        pool-name: FebsHikariCP
      # 配置默认数据源
      primary: primary
      datasource:
        # 数据源-1，名称为 primary
        primary:
          username: root
          password: hongqianhui1218
          driver-class-name: com.mysql.cj.jdbc.Driver
          url: jdbc:mysql://localhost:3306/packet_master_web?useUnicode=true&characterEncoding=UTF-8&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
        # 数据源-2，名称为 test
#        test:
#          username: root
#          password: 123456
#          driver-class-name: com.mysql.cj.jdbc.Driver
#          url: jdbc:mysql://127.0.0.1:3306/febs_test?useUnicode=true&characterEncoding=UTF-8&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
#  mvc :
#    static-path-pattern: front-end/**
  aop:
    proxy-target-class: true
  boot:
    admin:
      client:
        url : http://${myconfig.global_address}:${myconfig.admin_server_port}
        instance:
          prefer-ip: true
          service-base-url: http://${myconfig.global_address}:${myconfig.packet_master_web_port}
  kafka:
    producer:
      bootstrap-servers: http://${myconfig.kafka-server-url}
      retries: 1
      batch-size: 40960
      buffer-memory: 40960
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

#  redis:
#    host: 127.0.0.1
#    port: 6379
#    password:
#    jedis:
#      pool:
#        min-idle: 8
#        max-idle: 500
#        max-active: 2000
#        max-wait: 10000
#    timeout: 0


#mybatis plus 设置
mybatis-plus:
  type-aliases-package: com.zjucsc.application.domain
  mapper-locations: classpath:mapper/*.xml
  configuration:
    jdbc-type-for-null: null
  global-config:
    # 关闭 mybatis-plus的 banner
    banner: false

#febs:
#  # 是否异步记录用户操作日志
#  openAopLog: true
#  # 单次最大批量入库数量
#  max:
#    batch:
#      insert:
#        num: 1000
#  shiro:
#    # 后端免认证接口 url
#    anonUrl: /login,/logout/**,/regist,/user/check/**
#    # token有效期，单位秒
#    jwtTimeOut: 3600


hessian_service_config :
  hessian_url : localhost:8090
    art_history_data : http://${hessian_service_config.hessian_url}/artHistory
